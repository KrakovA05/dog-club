name: Keep Alive

on:
  schedule:
    # Каждые 5 минут — не даём контейнеру уснуть
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl -s -o /dev/null -w "%{http_code} %{time_total}s" https://lapaclub.ru

meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: null
  command: ""
  args: null
  persistenceMount: /data
  containerPort: "3000"
  servicePort: "80"

meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: null
  command: npm start
  args: null
  persistenceMount: /data
  containerPort: "3000"
  servicePort: "80"
